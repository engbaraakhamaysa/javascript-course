let product = ["Keybord", "Mouse", "Pen", "Pad", "Monitor"];

let index = 0;

while (index < product.length) {
  console.log(product[index]);
  index++;
}

index = 0;
while (index < 10) {
  console.log(index);
  index++;
}

index = 0;

while (true) {
  console.log(index);
  index++;
  if (index === 3) break;
}
